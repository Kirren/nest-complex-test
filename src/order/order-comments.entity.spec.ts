import { OrderCommentsEntity } from './order-comments.entity';

describe('OrderComments.Entity', () => {
  it('should be defined', () => {
    expect(new OrderCommentsEntity()).toBeDefined();
  });
});
